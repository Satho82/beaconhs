import { and, eq, isNull } from 'drizzle-orm'
import { notFound } from 'next/navigation'
import Link from 'next/link'
import { Button, EmptyState, Input, Label, PageHeader } from '@beaconhs/ui'
import { hospitalityFloors, hospitalityRooms } from '@beaconhs/db/schema'
import { requireRequestContext } from '@/lib/auth'
import { assertTenantModuleEntitled } from '@/lib/module-entitlements/server'
import { assertCan } from '@beaconhs/tenant'
import { can } from '@beaconhs/tenant'
import { createRoomAction, updateFloorAction } from '../../../../../actions'
export default async function FloorPage({params}:{params:Promise<{propertyId:string;buildingId:string;floorId:string}>}){const ctx=await requireRequestContext();await assertTenantModuleEntitled(ctx,'hospitality.properties');assertCan(ctx,'hospitality.read');const {propertyId,buildingId,floorId}=await params;const d=await ctx.db(async tx=>{const[floor]=await tx.select().from(hospitalityFloors).where(and(eq(hospitalityFloors.tenantId,ctx.tenantId),eq(hospitalityFloors.id,floorId),eq(hospitalityFloors.buildingId,buildingId),isNull(hospitalityFloors.deletedAt))).limit(1);return{floor,rooms:floor?await tx.select().from(hospitalityRooms).where(and(eq(hospitalityRooms.tenantId,ctx.tenantId),eq(hospitalityRooms.floorId,floorId),isNull(hospitalityRooms.deletedAt))):[]}});if(!d.floor)notFound();const manage=can(ctx,'hospitality.manage');return <main className="mx-auto max-w-5xl p-4"><PageHeader title={d.floor.name} description={d.floor.code}/>{manage&&<><form action={updateFloorAction} className="my-4 grid gap-2 rounded border p-3 sm:grid-cols-3"><input type="hidden" name="propertyId" value={propertyId}/><input type="hidden" name="buildingId" value={buildingId}/><input type="hidden" name="floorId" value={floorId}/><Label>Name<Input name="name" defaultValue={d.floor.name} required/></Label><Label>Code<Input name="code" defaultValue={d.floor.code} required/></Label><Button type="submit">Save changes</Button></form><form action={createRoomAction} className="my-4 grid gap-2 rounded border p-3 sm:grid-cols-4"><input type="hidden" name="propertyId" value={propertyId}/><input type="hidden" name="buildingId" value={buildingId}/><input type="hidden" name="floorId" value={floorId}/><Label>Number<Input name="code" required/></Label><Label>Name<Input name="name"/></Label><Label>Type<Input name="roomType" placeholder="room or apartment"/></Label><Button type="submit">Add Room/Apartment</Button></form></>}{d.rooms.length?d.rooms.map(r=><Link className="block border p-3" href={`/hospitality/properties/${propertyId}/buildings/${buildingId}/floors/${floorId}/rooms/${r.id}`} key={r.id}>{r.code} · {r.roomType??'room'}</Link>):<EmptyState title="No rooms or apartments" description="Rooms will appear here when added."/>}</main>}
