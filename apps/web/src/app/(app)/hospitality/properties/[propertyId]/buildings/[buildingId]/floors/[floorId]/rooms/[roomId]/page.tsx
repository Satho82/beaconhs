import { and, eq, isNull } from 'drizzle-orm'
import { notFound } from 'next/navigation'
import { Button, Input, Label, PageHeader } from '@beaconhs/ui'
import { hospitalityRooms, maintenanceIssues } from '@beaconhs/db/schema'
import Link from 'next/link'
import { requireRequestContext } from '@/lib/auth'
import { assertTenantModuleEntitled } from '@/lib/module-entitlements/server'
import { assertCan, can } from '@beaconhs/tenant'
import { updateRoomAction } from '../../../../../../actions'
import { reportMaintenanceIssueAction } from '../../../../../../actions'
export default async function RoomPage({params}:{params:Promise<{propertyId:string;buildingId:string;floorId:string;roomId:string}>}){const ctx=await requireRequestContext();await assertTenantModuleEntitled(ctx,'hospitality.properties');assertCan(ctx,'hospitality.read');const p=await params;const d=await ctx.db(async tx=>{const[row]=await tx.select().from(hospitalityRooms).where(and(eq(hospitalityRooms.tenantId,ctx.tenantId),eq(hospitalityRooms.id,p.roomId),eq(hospitalityRooms.floorId,p.floorId),isNull(hospitalityRooms.deletedAt))).limit(1);return{row,issues:row?await tx.select().from(maintenanceIssues).where(and(eq(maintenanceIssues.tenantId,ctx.tenantId),eq(maintenanceIssues.roomId,p.roomId))):[]}});if(!d.row)notFound();const row=d.row;return <main className="mx-auto max-w-5xl p-4"><PageHeader title={row.name||row.code} description={`${row.roomType??'room'} · ${row.status}`}/>{can(ctx,'hospitality.manage')&&<><form action={updateRoomAction} className="my-4 grid gap-2 rounded border p-3 sm:grid-cols-3"><input type="hidden" name="propertyId" value={p.propertyId}/><input type="hidden" name="buildingId" value={p.buildingId}/><input type="hidden" name="floorId" value={p.floorId}/><input type="hidden" name="roomId" value={p.roomId}/><Label>Number<Input name="code" defaultValue={row.code} required/></Label><Label>Name<Input name="name" defaultValue={row.name??''}/></Label><Label>Type<Input name="roomType" defaultValue={row.roomType??''}/></Label><Button type="submit">Save changes</Button></form><form action={reportMaintenanceIssueAction} className="grid gap-2 rounded border p-3"><input type="hidden" name="roomId" value={p.roomId}/><Label>Issue title<Input name="title" required/></Label><Label>Description<Input name="description"/></Label><Label>Priority<Input name="priority" defaultValue="medium" required/></Label><Button type="submit">Report Maintenance Issue</Button></form></>}<section className="mt-4">{d.issues.map(i=><Link className="block border p-3" href={`/hospitality/maintenance/${i.id}`} key={i.id}>{i.summary} · {i.priority} · {i.status}</Link>)}</section></main>}
