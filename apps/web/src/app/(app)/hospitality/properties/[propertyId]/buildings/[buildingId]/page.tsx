import { and, eq, isNull } from 'drizzle-orm'
import { notFound } from 'next/navigation'
import { EmptyState, PageHeader } from '@beaconhs/ui'
import { hospitalityBuildings, hospitalityFloors, hospitalityProperties } from '@beaconhs/db/schema'
import { requireRequestContext } from '@/lib/auth'
import { assertTenantModuleEntitled } from '@/lib/module-entitlements/server'
import { assertCan } from '@beaconhs/tenant'
export default async function BuildingPage({params}:{params:Promise<{propertyId:string;buildingId:string}>}) { const ctx=await requireRequestContext();await assertTenantModuleEntitled(ctx,'hospitality.properties');assertCan(ctx,'hospitality.read');const {propertyId,buildingId}=await params;const d=await ctx.db(async tx=>{const [property]=await tx.select().from(hospitalityProperties).where(and(eq(hospitalityProperties.tenantId,ctx.tenantId),eq(hospitalityProperties.id,propertyId),isNull(hospitalityProperties.deletedAt))).limit(1);const [building]=property?await tx.select().from(hospitalityBuildings).where(and(eq(hospitalityBuildings.tenantId,ctx.tenantId),eq(hospitalityBuildings.id,buildingId),eq(hospitalityBuildings.propertyId,propertyId),isNull(hospitalityBuildings.deletedAt))).limit(1):[];const floors=building?await tx.select().from(hospitalityFloors).where(and(eq(hospitalityFloors.tenantId,ctx.tenantId),eq(hospitalityFloors.buildingId,buildingId),isNull(hospitalityFloors.deletedAt))):[];return{property,building,floors}});if(!d.building)notFound();return <main className="mx-auto max-w-5xl p-4"><PageHeader title={d.building.name} description={`${d.property?.name} · ${d.building.code}`}/>{d.floors.length?d.floors.map(f=><div className="border p-3" key={f.id}>{f.name}</div>):<EmptyState title="No floors" description="Add a floor to continue the hierarchy."/>}</main> }
