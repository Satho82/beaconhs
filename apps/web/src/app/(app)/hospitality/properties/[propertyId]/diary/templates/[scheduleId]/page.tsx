import Link from 'next/link'
import { and, eq } from 'drizzle-orm'
import { Button, Input, Label, PageHeader, Select } from '@beaconhs/ui'
import { can } from '@beaconhs/tenant'
import { tenantUsers, users } from '@beaconhs/db/schema'
import { requireRequestContext } from '@/lib/auth'
import { getDiaryTemplate } from '@/lib/hospitality/diary'
import { setDiaryTemplateActiveAction, updateDiaryTemplateAction } from '../../actions'

export default async function DiaryTemplatePage({ params }: { params: Promise<{ propertyId: string; scheduleId: string }> }) {
  const ctx = await requireRequestContext(); const { propertyId, scheduleId } = await params
  const { property, schedule, template } = await getDiaryTemplate(ctx, propertyId, scheduleId)
  const members = await ctx.db((tx) => tx.select({ id: tenantUsers.id, name: tenantUsers.displayName, email: users.email }).from(tenantUsers).innerJoin(users, eq(users.id, tenantUsers.userId)).where(and(eq(tenantUsers.tenantId, ctx.tenantId), eq(tenantUsers.status, 'active'))))
  const recurrence = schedule.recurrence as { kind?: string; cron?: string }
  const parts = recurrence.cron?.split(' ') ?? []
  const dueTime = parts.length === 5 ? `${String(parts[1]).padStart(2, '0')}:${String(parts[0]).padStart(2, '0')}` : '09:00'
  const manage = can(ctx, 'hospitality.manage')
  return <main className="mx-auto max-w-3xl p-4 sm:p-6"><PageHeader title={template.title} description={`${property.name} · ${schedule.isActive ? 'Active' : 'Inactive'} · ${schedule.timezone}`} action={<Button asChild variant="outline"><Link href={`/hospitality/properties/${propertyId}/diary`}>Back to diary</Link></Button>}/><section className="mt-5 rounded-lg border p-4"><p className="text-sm">{template.instructions || 'No instructions provided.'}</p><dl className="mt-4 grid gap-2 text-sm sm:grid-cols-2"><div><dt className="text-muted-foreground">Recurrence</dt><dd>{recurrence.kind ?? 'custom'}</dd></div><div><dt className="text-muted-foreground">Due time</dt><dd>{dueTime}</dd></div><div><dt className="text-muted-foreground">Created</dt><dd>{template.createdAt.toLocaleString()}</dd></div><div><dt className="text-muted-foreground">Last updated</dt><dd>{template.updatedAt.toLocaleString()}</dd></div></dl></section>{manage && <section className="mt-5 rounded-lg border p-4"><h2 className="font-semibold">Edit template</h2><form action={updateDiaryTemplateAction} className="mt-3 grid gap-3 sm:grid-cols-2"><input type="hidden" name="propertyId" value={propertyId}/><input type="hidden" name="scheduleId" value={scheduleId}/><Label>Title<Input name="title" defaultValue={template.title} required/></Label><Label>Due time<Input name="dueTime" type="time" defaultValue={dueTime} required/></Label><Label className="sm:col-span-2">Instructions<Input name="instructions" defaultValue={template.instructions ?? ''}/></Label><Label>Recurrence<Select name="recurrence" defaultValue={recurrence.kind ?? 'daily'}><option value="daily">Daily</option><option value="weekly">Weekly</option><option value="monthly">Monthly</option></Select></Label><Label>Weekday<Select name="weekday" defaultValue={parts[4] ?? '1'}><option value="0">Sunday</option><option value="1">Monday</option><option value="2">Tuesday</option><option value="3">Wednesday</option><option value="4">Thursday</option><option value="5">Friday</option><option value="6">Saturday</option></Select></Label><Label>Month day<Input name="monthDay" type="number" min="1" max="28" defaultValue={parts[2] ?? '1'}/></Label><Label>Timezone<Input name="timezone" defaultValue={schedule.timezone} required/></Label><Label>Assignee<Select name="assigneeId" defaultValue={schedule.assignedToTenantUserId ?? ''}><option value="">Unassigned</option>{members.map((member) => <option key={member.id} value={member.id}>{member.name || member.email}</option>)}</Select></Label><div className="flex items-end"><Button type="submit">Save changes</Button></div></form><form action={setDiaryTemplateActiveAction} className="mt-4"><input type="hidden" name="propertyId" value={propertyId}/><input type="hidden" name="scheduleId" value={scheduleId}/><input type="hidden" name="isActive" value={schedule.isActive ? 'false' : 'true'}/><Button type="submit" variant="outline">{schedule.isActive ? 'Disable template' : 'Enable template'}</Button></form></section>}</main>
}
